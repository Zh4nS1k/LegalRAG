name: Legally CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- GO (Backend) ---
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Go Format Check
        run: |
          cd server
          if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then
            echo "The following files are not formatted:"
            gofmt -l .
            exit 1
          fi

      - name: Install Go Dependencies
        run: cd server && go mod download

      - name: Run Go Tests
        run: cd server && go test ./tests/... ./api/controllers/... -v

      # --- PYTHON (AI Engine) ---
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest pytest-mock pytest-asyncio
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Lint with flake8
        run: |
          # stop the build if there are Python syntax errors or undefined names
          flake8 engine --count --select=E9,F63,F7,F82 --show-source --statistics
          # exit-zero treats all errors as warnings.
          flake8 engine --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Run Python Tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pytest engine/tests
        env:
          PINECONE_API_KEY: "dummy-key-for-ci"
          GROQ_API_KEY: "dummy-key-for-ci"

      # --- NODE.JS (Frontend) ---
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install Node Dependencies
        run: cd client && npm install

      - name: Frontend Lint
        run: cd client && npm run lint || echo "Lint failed but continuing"

      - name: Run Frontend Tests
        run: cd client && npm test -- --watchAll=false

  docker-build-check:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Build Backend Image
        run: docker build -t legally-backend ./server

      - name: Build AI Engine Image
        run: docker build -t legally-ai ./engine
